apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: rhcl-operator
  namespace: openshift-operators
spec:
  channel: "{{ ocp4_workload_openshift_ai_maas_kuadrant_channel }}"
  installPlanApproval: Manual
  name: rhcl-operator
  source: redhat-operators
  sourceNamespace: openshift-marketplace
{% if ocp4_workload_openshift_ai_maas_kuadrant_starting_csv | default('') | length > 0 %}
  startingCSV: "{{ ocp4_workload_openshift_ai_maas_kuadrant_starting_csv }}"
{% endif %}
